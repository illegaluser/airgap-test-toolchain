[pytest]
testpaths = .
pythonpath = ..
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    e2e: 통합 E2E 슈트 (별도 데몬 spawn + Playwright 브라우저). pre-commit 에서 `-m e2e` 로 일괄 실행.
